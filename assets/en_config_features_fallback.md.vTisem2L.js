import{St as e,at as t,it as n,ot as r,st as i,t as a,tt as o,vt as s}from"./chunks/framework.B8HA7oqK.js";var c=JSON.parse(`{"title":"Fallback","description":"","frontmatter":{},"headers":[],"relativePath":"en/config/features/fallback.md","filePath":"en/config/features/fallback.md","lastUpdated":1779716251000}`),l={name:`en/config/features/fallback.md`},u={id:`fallbacks-design-theory`,tabindex:`-1`};function d(a,c,l,d,f,p){let m=e(`Badge`);return s(),n(`div`,null,[c[3]||=t(`<h1 id="fallback" tabindex="-1">Fallback <a class="header-anchor" href="#fallback" aria-label="Permalink to “Fallback”">​</a></h1><blockquote><p><strong>Fallback is one of Xray&#39;s most powerful features, effectively preventing active probing and allowing multiple services to share common ports.</strong></p></blockquote><p>Fallback provides Xray with high-strength resistance against active probing and features a unique first-packet fallback mechanism.</p><p>Fallback can also split different types of traffic based on <code>path</code>, allowing a single port to be shared by multiple services.</p><p>Currently, you can use the fallback feature by configuring <code>fallbacks</code> when using VLESS or Trojan protocols, allowing for very rich configuration combinations.</p><h2 id="fallbacks-configuration" tabindex="-1">Fallbacks Configuration <a class="header-anchor" href="#fallbacks-configuration" aria-label="Permalink to “Fallbacks Configuration”">​</a></h2><div class="language-json line-numbers-mode"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes dark-plus light-plus" style="--shiki-dark:#D4D4D4;--shiki-light:#000000;--shiki-dark-bg:#1E1E1E;--shiki-light-bg:#FFFFFF;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">{</span></span>
<span class="line"><span style="--shiki-dark:#9CDCFE;--shiki-light:#0451A5;">  &quot;fallbacks&quot;</span><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">: [</span></span>
<span class="line"><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">    {</span></span>
<span class="line"><span style="--shiki-dark:#9CDCFE;--shiki-light:#0451A5;">      &quot;dest&quot;</span><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">: </span><span style="--shiki-dark:#B5CEA8;--shiki-light:#098658;">80</span></span>
<span class="line"><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">    }</span></span>
<span class="line"><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">  ]</span></span>
<span class="line"><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><blockquote><p><code>fallbacks</code>: [ <a href="#fallbackobject">FallbackObject</a> ]</p></blockquote><p>An array containing a series of powerful fallback distribution configurations.</p><h3 id="fallbackobject" tabindex="-1">FallbackObject <a class="header-anchor" href="#fallbackobject" aria-label="Permalink to “FallbackObject”">​</a></h3><div class="language-json line-numbers-mode"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes dark-plus light-plus" style="--shiki-dark:#D4D4D4;--shiki-light:#000000;--shiki-dark-bg:#1E1E1E;--shiki-light-bg:#FFFFFF;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">{</span></span>
<span class="line"><span style="--shiki-dark:#9CDCFE;--shiki-light:#0451A5;">  &quot;name&quot;</span><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">: </span><span style="--shiki-dark:#CE9178;--shiki-light:#A31515;">&quot;&quot;</span><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">,</span></span>
<span class="line"><span style="--shiki-dark:#9CDCFE;--shiki-light:#0451A5;">  &quot;alpn&quot;</span><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">: </span><span style="--shiki-dark:#CE9178;--shiki-light:#A31515;">&quot;&quot;</span><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">,</span></span>
<span class="line"><span style="--shiki-dark:#9CDCFE;--shiki-light:#0451A5;">  &quot;path&quot;</span><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">: </span><span style="--shiki-dark:#CE9178;--shiki-light:#A31515;">&quot;&quot;</span><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">,</span></span>
<span class="line"><span style="--shiki-dark:#9CDCFE;--shiki-light:#0451A5;">  &quot;dest&quot;</span><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">: </span><span style="--shiki-dark:#B5CEA8;--shiki-light:#098658;">80</span><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">,</span></span>
<span class="line"><span style="--shiki-dark:#9CDCFE;--shiki-light:#0451A5;">  &quot;xver&quot;</span><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">: </span><span style="--shiki-dark:#B5CEA8;--shiki-light:#098658;">0</span></span>
<span class="line"><span style="--shiki-dark:#D4D4D4;--shiki-light:#000000;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p><strong><code>fallbacks</code> is an array; this is the configuration description for one of its child elements.</strong></p><p>The <code>fallbacks</code> item is optional and can only be used with the TCP+TLS transport combination.</p><ul><li>When this item has child elements, <a href="./../transports/tls.html#tlsobject">Inbound TLS</a> must set <code>&quot;alpn&quot;:[&quot;http/1.1&quot;]</code>.</li></ul><p>Usually, you need to first set a default fallback with both <code>alpn</code> and <code>path</code> omitted or empty, and then configure other traffic splitting as needed.</p><p>VLESS will forward traffic to the address specified by <code>dest</code> if, after TLS decryption, the first packet length is &lt; 18, the protocol version is invalid, or authentication fails.</p><p>For other transport combinations, the <code>fallbacks</code> item or all child elements must be deleted. In this case, Fallback will not be enabled. VLESS will wait to read the required length, and if the protocol version is invalid or authentication fails, it will directly disconnect.</p><blockquote><p><code>name</code>: string</p></blockquote><p>Attempts to match TLS SNI (Server Name Indication). Empty means any. Default is <code>&quot;&quot;</code>.</p><blockquote><p><code>alpn</code>: string</p></blockquote><p>Attempts to match the TLS ALPN negotiation result. Empty means any. Default is <code>&quot;&quot;</code>.</p><p>Xray will only attempt to read the TLS ALPN negotiation result when necessary. If successful, it outputs <code>realAlpn =</code> to the info log. Usage: Solves the issue where Nginx&#39;s h2c service cannot be compatible with http/1.1 simultaneously. Nginx would require two <code>listen</code> lines, for 1.1 and h2c respectively. Note: When <code>fallbacks</code> <code>alpn</code> contains <code>&quot;h2&quot;</code>, <a href="./../transports/tls.html#tlsobject">Inbound TLS</a> needs to set <code>&quot;alpn&quot;:[&quot;h2&quot;,&quot;http/1.1&quot;]</code> to support h2 access.</p><div class="tip custom-block"><p class="custom-block-title custom-block-title-default">TIP</p><p>The <code>alpn</code> set in Fallback matches the <em>actually negotiated</em> ALPN, whereas the <code>alpn</code> set in Inbound TLS is the list of <em>optional</em> ALPNs during the handshake. The meanings are different.</p></div><blockquote><p><code>path</code>: string</p></blockquote><p>Attempts to match the HTTP PATH of the first packet. Empty means any. Default is empty. If non-empty, it must start with <code>/</code>. h2c is not supported.</p><p>Smart: Xray will only attempt to peek at the PATH when necessary (not exceeding 55 bytes; uses the fastest algorithm, does not fully parse HTTP). If successful, it outputs the INFO log <code>realPath =</code>. Usage: Offloading WebSocket traffic or HTTP camouflage traffic from other inbounds. It performs pure traffic forwarding without extra processing. Theoretical performance is stronger than Nginx.</p><p>Note: <strong>The inbound where fallbacks is located must itself be TCP+TLS</strong>. This is used for offloading to other WS inbounds; the offloaded inbound does not need to configure TLS.</p><blockquote><p><code>dest</code>: string | number</p></blockquote><p>Decides the destination of the TCP traffic after TLS decryption. Currently supports two types of addresses: (This item is mandatory, otherwise it will not start)</p><ol><li>TCP, formatted as <code>&quot;addr:port&quot;</code>, where <code>addr</code> supports IPv4, domain name, and IPv6. If a domain name is filled, a TCP connection will be initiated directly (without going through the built-in DNS).</li><li>Unix domain socket, formatted as an absolute path, like <code>&quot;/dev/shm/domain.socket&quot;</code>. Can handle <a href="https://www.man7.org/linux/man-pages/man7/unix.7.html" target="_blank" rel="noreferrer">abstract</a> by adding <code>@</code> at the beginning, or <code>@@</code> for abstract with padding.</li></ol><p>If only <code>port</code> is filled, it can be a number or a string, like <code>80</code> or <code>&quot;80&quot;</code>. It usually points to a cleartext http service (<code>addr</code> will be filled as <code>&quot;localhost&quot;</code>).</p><p>Note: Only after v25.7.26 does a <code>dest</code> containing only a port point to <code>localhost</code>. Before this, it was <code>127.0.0.1</code>. After the change, the actual target is likely <code>::1</code>. Some webserver templates copied online might listen on <code>::1</code> but only allow <code>127</code> to enter or apply the proxy protocol, which may lead to different behaviors.</p><blockquote><p><code>xver</code>: number</p></blockquote><p>Sends <a href="https://www.haproxy.org/download/2.2/doc/proxy-protocol.txt" target="_blank" rel="noreferrer">PROXY protocol</a>, specifically used to pass the real source IP and port. Fill 1 or 2. Default is 0, meaning it is not sent. It is recommended to fill 1 if needed.</p><p>Currently, filling 1 or 2 functions identically, only the structure differs (the former is printable, the latter is binary). Both TCP and WS inbounds in Xray support receiving PROXY protocol.</p><div class="warning custom-block"><p class="custom-block-title custom-block-title-default">WARNING</p><p>If you are <a href="https://docs.nginx.com/nginx/admin-guide/load-balancer/using-proxy-protocol/#configuring-nginx-to-accept-the-proxy-protocol" target="_blank" rel="noreferrer">configuring Nginx to accept PROXY protocol</a>, besides setting <code>proxy_protocol</code>, you also need to set <code>set_real_ip_from</code>, otherwise issues may occur.</p></div><h3 id="supplementary-explanation" tabindex="-1">Supplementary Explanation <a class="header-anchor" href="#supplementary-explanation" aria-label="Permalink to “Supplementary Explanation”">​</a></h3><ul><li>It will match the most precise child element, regardless of the order of child elements. If several child elements with identical <code>alpn</code> and <code>path</code> are configured, the last one will prevail.</li><li>Fallback offloading is forwarding at the decrypted TCP layer, not the HTTP layer. It only checks the first packet&#39;s PATH when necessary.</li><li>You can view more tips and insights on using Fallbacks here: <ul><li><a href="./../../document/level-1/fallbacks-lv1.html">Analysis of Fallbacks Features</a></li></ul></li></ul>`,38),o(`h2`,u,[c[0]||=r(`Fallbacks Design Theory `,-1),i(m,{text:`WIP`,type:`warning`}),c[1]||=r(),c[2]||=o(`a`,{class:`header-anchor`,href:`#fallbacks-design-theory`,"aria-label":`Permalink to “Fallbacks Design Theory”`},`​`,-1)])])}var f=a(l,[[`render`,d]]);export{c as __pageData,f as default};